var command = Null;
function commands(command, args) {
    
    switch (command) {
        case system:
            break;
        case game:
            break;
        default:
            break;
    }
}