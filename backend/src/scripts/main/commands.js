var command = Null;
var test = false;
function commands(/*command, args*/ test) {

    if (test) {
        
    }

    /*
    switch (command) {
        case system:
            break;
        case game:
            break;
        default:
            break;
    }*/
}